const Algo = require("../algo");

describe("Algo", () => {
  describe("reverse", () => {
    it("should return a reversed string", () => {
      const str = 'Hello World!';
      const reversed = '!dlroW olleH'

      const result = new Algo().reverse(str);

      expect(result).toEqual(reversed)
     
  });

});

  describe("isPalindrome", () => {
    it("should return a string containing a palindrome when called with the 'isPalindrome' keyword", () => {
      const isPalindrome = algo.isPalindrome(str2);

      expect("string" in obj).toEqual('racacar');
    });

  });

  
  describe("capitalize", () => {
    it("should return a string containing a string that is capitalized when called with the 'capitalized' keyword", () => {
      const capitalized = algo.capitalize(str3);

      expect("string" in obj).toEqual('Once Upon A Time There Were Three Little Pigs');

    });

  });

  });

